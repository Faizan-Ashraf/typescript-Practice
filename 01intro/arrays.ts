const heros: string[] = []
// const power: number[] = []

const power: Array<number> = []

heros.push("superman")

type User = {
    name: string
    isActive: boolean
}
const allUsers: User[] = []
allUsers.push({name: "", isActive: true})